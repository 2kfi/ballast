# built binary
/ballast
/ballast-*

# local data + logs (never commit registry contents or secrets)
data/
*.log
audit.log

# real config (commit only ballast.example.json)
ballast.json

# editor / OS noise
.DS_Store
.idea/
.vscode/
